packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks postinstall scripts by default. These three legitimately need
# them (native binaries), everything else stays blocked.
onlyBuiltDependencies:
  - "@tailwindcss/oxide"
  - esbuild
  - sharp
  - unrs-resolver
